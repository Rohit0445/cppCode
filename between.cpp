#include <iostream>
using namespace std;
int main (){

    int num;
    cout<<"enter a number";
    cin>>num;
    (num<0 && num>50)?cout<<"Number is not in between 0 to 50":cout<<"Number is  in between 0 to 50";
}
